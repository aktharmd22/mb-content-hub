; Raise upload limits so tech team can submit larger reels/videos.
; Hostinger (PHP-FPM) honors per-directory .user.ini in the document root.
upload_max_filesize = 256M
post_max_size = 256M
max_file_uploads = 30
max_execution_time = 300
memory_limit = 512M
